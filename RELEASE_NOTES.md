Windows x64 与 Apple 芯片 Mac 现使用相同的 `2026-09-08.12` 采集面板：标题、中文学期、进度与按钮分区，版本移至底部，长说明可以展开。包含 Safari 安装和下载指引，以及此前 Mac 的保存位置恢复、Finder 定位、安全退出和窗口修复。

下载 `Windows-x64-Mac-arm64.zip` 可一次获得两端软件及统一使用说明；也可选择单独的 Windows 或 Mac ZIP。两端均内置运行环境，无需安装 Python。

升级前先退出旧助手，保留原课表保存目录、UID 和历史。**更新软件后，仍须在实际采集的浏览器中手动替换一次书签**；新面板底部应显示 `2026-09-08.12`。更新 APP/EXE 不会自动修改浏览器保存的旧按钮。

本候选版的发布以 Windows 和 Mac 各自完整检查、冻结程序自检、包内 `.12` 安装页检查以及相同源码指纹和书签地址校验通过为前提。Mac ZIP 另经中文和空格路径重新解压、签名结构与冻结程序自检；附件提供 `SHA256SUMS.txt` 和 `build-verification.json`。个人课表、学校原始数据、配置及本机日志不随源码或软件包发布。

本次没有重新采集学校数据。`.11` 的 Safari 真实采集和旧包人工验收属于历史证据；不作为 rc12 的学校、手机或另一台个人电脑验收结果。Windows 实际缩放、原生文件选择与手机导入等仍需按 `STUDENT_ACCEPTANCE.md` 单独确认。

Mac 仅面向 Apple 芯片，尚无 Developer ID 签名和 Apple 公证。首次打开可能需要使用者按包内说明手动允许；不要关闭系统安全检查。Intel Mac 不在本次范围内。
