# 使用体验与恢复

2026-09-06：以下场景来自现有代码和使用流程排查。修补使用合成数据、临时目录和模拟网络验证；其他人的账号、电脑和手机仍需实际使用核对。

| 可能遇到的情况 | 修补后的行为 |
| --- | --- |
| 下载 GitHub ZIP 后，中文批处理报命令不存在 | `.cmd` 在 Git 内保留 CRLF 换行，中文和空格目录中的真实 CMD 入口纳入验证 |
| 没有 `py` 启动器、Python 太旧、依赖下载失败，窗口闪退 | 安装器检查 `py` / `python` 和版本，保留失败提示；可重新运行，原文件保留 |
| 书签生成失败，却看到“安装完成” | 检查生成命令的退出码，仅全部成功才报完成 |
| 先点击书签再开接收窗口，或另存到其他目录 | 双击“导入已下载课表.cmd”选择文件，或把 JSON 拖到“同步课表.cmd”上，无需重新采集 |
| 覆盖同名下载文件、文件正在写入、浏览器只下载了诊断 | 检测大小和修改时间，等待稳定；识别诊断并提示查看 Chrome，继续等完整文件 |
| 采集成功却没保存 JSON，又要等几分钟重采 | 结果面板可重新下载同一次采集，请求数量和采集时间不增加；关闭面板会清除保留结果 |
| 配置带 BOM、日期无效、JSON 写错、旧书签学期不符 | 支持 UTF-8 BOM、环境变量及相对下载目录；明确指出配置问题和更新书签的步骤 |
| 把诊断文件、损坏 JSON 或旧采集当成课表导入 | 明确拒绝并给出下一步，保留当前课表；同一文件重复导入不产生新版本 |
| 两人混用目录，误把 `--new-term` 当作换账号 | 即使传入该参数也拒绝账号变化；每人使用独立目录和各自的上传配置 |
| 同一学期一直带着 `--new-term`，课程修订历史被重置 | 范围未改变时仍沿用已有 UID、历史标识和修订号 |
| 未启用 WebCal 却以为手机会更新，或上传配置损坏后误以为本地同步也失败 | 明确显示本地处理状态；发布配置错误返回退出码 4，并提示修正后仅重试上传 |
| 其他人的 WakeUp 作息不同，或同步后忘记重新导出 | 支持 `local/wakeup-slots.json`；每次仍校验实际起止时间；课程变化时提醒旧 CSV 需重新导出 |
| 新安装没有历史快照，却提示“已恢复” | 明确提示尚无课表可恢复，需要先完成一次采集 |
| 把下载或诊断 JSON 放在源码目录，误提交到 Git | 默认忽略 `shsmu-capture-*.json` 和 `shsmu-diagnostic-*.json`；改名后的个人文件仍需自行保管 |

## 已下载文件的最短恢复步骤

1. 如果“同步课表”窗口仍在等待，先关闭它。
2. 双击“导入已下载课表.cmd”，选择完整的 `shsmu-capture-*.json`；也可直接拖到“同步课表.cmd”上。
3. 查看本地同步结果。WebCal 上传未确认时，修正配置或网络问题后双击“仅上传日历.cmd”。

文件选择组件不可用时，提示改用拖入方式。Chrome 显示采集完成不能证明文件已经落盘，上传成功也不能证明手机立即刷新；各阶段分别显示结果。

## 更新与回退

保留个人目录和配置，更新源码后运行 `setup.cmd`。刷新 `chrome-bookmark.html`，手动将旧书签网址替换成修正版 6；程序不会自动改动 Chrome。修正版 5 的完整采集 JSON 仍可导入。

需要回退程序时，恢复自己备份的旧源码及书签，不重置 `data/current.json` 或删除历史运行。新导入入口和自定义作息只是本地功能，服务器协议未改变。

## 仍存在的使用边界

- 需要本人完成教务登录；网络、会话过期或学校更改字段仍可能使采集停止。
- 整学期为空、考试或其他尚未核实的非空数据分支仍会停止，不把未知内容当作删课。
- 自定义 WakeUp 作息目前仍为 1–14 节；实际时间或教学周次冲突时不强行转换。
- 学校真实调课后的手机刷新延迟和删除、WakeUp 重复导入覆盖、其他账号实机效果仍待验证。
