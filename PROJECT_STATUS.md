# 项目状态

发布整理日期：2026-09-06。

本仓库提供 Chrome 书签采集、本地完整性检查、课表标准化、稳定 UID、差异记录和 ICS 导出，以及可选 WebCal 上传与服务端。

核心采集代码源自已完成真实验收的本地版本。发布副本的同步和浏览器业务代码保持一致；使用说明已改为通用安装步骤，移除了个人数据、部署信息和专用安装器。仓库使用独立 Git 历史，未携带原工作目录的历史提交。

日常入口为 `setup.cmd`（首次）及 `同步课表.cmd`（每天）。先启动接收窗口，再点击已有 Chrome 书签。未配置 WebCal 时不访问日历服务器。

2026-09-06 新增独立的 `导出 WakeUp 课表.cmd` / `wakeup.py`：从当前完整快照及原始教师详情离线生成 WakeUp 七列 CSV 和作息说明。手动导入，不自动更新；不修改现有同步或 WebCal 流程，不新增依赖。一个账号已完成全量转换核对和重复导出验证，用户确认 iOS WakeUp 可正常使用。作息中推算的边界在说明中标记，重复导入覆盖与删除行为仍未验证。

已验证和未验证边界见 VERIFICATION.md。跨账号使用、学校后续实际调课后的身份稳定性及手机更新行为仍需实际验证。暂无无人值守学校采集、定时任务或消息通知。

## 2026-09-06 审查修复

已修复历史源标识导致的 WakeUp 导出误拒绝、空教师详情覆盖旧信息、课程编号变化漏报、课程类型更正导致 UID 更换，以及服务端接受日期损坏日历的问题。增加统一检查入口和完整 CLI 流程回归；新增大幅删课与整月变空的核对提示。

修正版 5 书签需使用者手动更新。代码发布与使用者各自的服务器升级、学校实采及手机验证分开记录，不能把模拟测试称为这些环境的实际验收。
