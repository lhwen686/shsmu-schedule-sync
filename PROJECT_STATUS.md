# 项目状态

> 当前学生桌面候选版：1.0.0-rc7。等待接收时醒目提醒点击浏览器课表书签；首页、结果页和 WakeUp 指引提醒按本次设置卡手动设置每节课时长与学期第一天；启动时用大白话说明安全软件提醒。WakeUp CSV / 苹果日历 ICS 继续独立导出。当前版本的学校、手机、另一台无 Python 电脑及两名同学实机验收仍待完成；见 STUDENT_ACCEPTANCE.md。

本版已将首次书签引导和配套 HTML 使用说明中的图片替换为标注地址栏、书签栏的原图，显示时按窗口宽度等比缩放。采集按钮版本为修正版 8。

此前版本补充浏览器通用指引和基础功能检查，适配目标包括 Edge、Chrome、Firefox；IE 兼容模式不支持。移除对 AbortSignal.timeout 的依赖，缺少 Fetch 取消能力时使用自带超时的 XHR。已安装书签须本人手动更新到修正版 8；其他浏览器及本版学校实采仍待验证，不能把模拟检查当作跨浏览器实采通过。

当前首次引导只选择学期，不要求个人结课日期。2026–2027 秋季默认读取到 2027-02-21（含），覆盖校历寒假、止于春季开学前；本次已公布课程起止日及 WakeUp 所需周数由实际课程确定。同账号、同学期扩大覆盖并完整重采时保留 UID、修订号、源标识历史和取消记录。该扩大范围尚未在学校实采，不能宣称所有实习或跨学期安排已支持。

发布整理日期：2026-09-07。学生版的 GitHub 预发布标识为 `v1.0.0-rc7`，软件与图文说明通过 Release 下载页提供；此前 rc6 和 rc4 保留。

当前安装页仍为修正版 8（2026-09-06.8），本次没有修改浏览器采集模块。RC7 已完成本地自动检查及开发机隔离示例数据的窗口核对；最终 9 月 7 日说明修订版已通过普通用户的包内自检，该次 PATH 含 Python。这些检查不代替学校采集、另一台电脑或手机验收；本轮发布检查详见 VERIFICATION.md。

此前修正版 6 修复 --new-term 绕过账号检查及重复参数重置修订历史的问题，增加可选 WakeUp 作息及旧 CSV 提醒，继续忽略个人下载文件。该轮 69 项 Python 模拟测试及两组 JavaScript 测试通过；既有课表回放没有变化。本次候选版仍待另一台无 Python 电脑、新书签学校实采、iPhone 和两名同学独立操作验收；已安装书签需手动更新。本轮无需后端升级。

本仓库提供浏览器书签采集、本地完整性检查、课表标准化、稳定 UID、差异记录和 ICS 导出，以及可选 WebCal 上传与服务端。

核心采集代码源自已完成真实验收的本地版本。发布副本的同步和浏览器业务代码保持一致；使用说明已改为通用安装步骤，移除了个人数据、部署信息和专用安装器。仓库使用独立 Git 历史，未携带原工作目录的历史提交。

学生日常入口为 `医学院课表助手.exe`，默认仅在本地处理。原命令行入口 `setup.cmd`（首次）及 `同步课表.cmd`（每天）继续可用，详见 MAINTENANCE.md；命令行未配置 WebCal 时不访问日历服务器。两种入口都先开始接收，再点击浏览器中的课表按钮。

2026-09-06 新增独立的 `导出 WakeUp 课表.cmd` / `wakeup.py`：从当前完整快照及原始教师详情离线生成 WakeUp 七列 CSV 和作息说明。手动导入，不自动更新；不修改现有同步或 WebCal 流程，不新增依赖。一个账号已完成全量转换核对和重复导出验证，用户确认 iOS WakeUp 可正常使用。作息中推算的边界在说明中标记，重复导入覆盖与删除行为仍未验证。

已验证和未验证边界见 VERIFICATION.md。跨账号使用、学校后续实际调课后的身份稳定性及手机更新行为仍需实际验证。暂无无人值守学校采集、定时任务或消息通知。

## 2026-09-06 审查修复

已修复历史源标识导致的 WakeUp 导出误拒绝、空教师详情覆盖旧信息、课程编号变化漏报、课程类型更正导致 UID 更换，以及服务端接受日期损坏日历的问题。增加统一检查入口和完整 CLI 流程回归；新增大幅删课与整月变空的核对提示。

修正版 5 书签需使用者手动更新。代码发布与使用者各自的服务器升级、学校实采及手机验证分开记录，不能把模拟测试称为这些环境的实际验收。

修复版本已在原维护环境完成后端升级、真实 HTTPS 回归、修正版 5 两次独立全学期采集、上传回读及重复无变化验证；使用者确认 10 条网页清单和手机显示一致。详细区分直接验证与用户手工反馈见 VERIFICATION.md，私人证据不随源码发布。
